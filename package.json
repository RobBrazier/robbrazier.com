{
  "name": "robbrazier.com",
  "private": true,
  "scripts": {
    "hugo": "./.hugo/hugo",
    "build": "npm run hugo -- --minify",
    "start": "npm run hugo -- server -D -E -F --port 8080 --bind 0.0.0.0 --renderToDisk -d public"
  },
  "hugo-bin": {
    "buildTags": "extended"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RobBrazier/robbrazier.com.git"
  },
  "author": "Rob Brazier",
  "devDependencies": {},
  "dependencies": {
    "milligram-scss": "^1.3.0"
  }
}
