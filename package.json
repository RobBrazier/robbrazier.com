{
  "name": "robbrazier.com",
  "private": true,
  "scripts": {
    "build": "hugo",
    "start": "hugo server -D -E -F --port 8080 --bind 0.0.0.0 --renderToDisk -d public",
    "serve": "static -a 0.0.0.0 -p 80 public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RobBrazier/robbrazier.com.git"
  },
  "author": "Rob Brazier",
  "dependencies": {
    "milligram-scss": "^1.3.0",
    "node-static": "^0.7.11"
  },
  "devDependencies": {
    "hugo-extended": "^0.68.2"
  }
}
