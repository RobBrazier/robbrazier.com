version: v1.0
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

blocks:
  - name: "Build"
    task:
      jobs:
      - name: Hugo Build
        commands:
          - checkout
          - cache delete robbrazier-$SEMAPHORE_GIT_SHA-$SEMAPHORE_GIT_BRANCH _site
          - cache restore
          - npm install
          - cache store
          - npm run build
          - cache store robbrazier-$SEMAPHORE_GIT_SHA-$SEMAPHORE_GIT_BRANCH _site

promotions:
  - name: Deploy
    pipeline_file: deploy.yml
    auto_promote_on:
      - result: passed
        branch:
          - master
